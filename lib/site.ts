// Real domain still pending (see README) — falls back to the live Vercel URL
// rather than a placeholder that resolves nowhere.
export const SITE_URL =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://threshold-gray.vercel.app";

export const SITE_NAME = "Threshold";

export const SITE_TAGLINE =
  "Direct experience, not more explanation.";
